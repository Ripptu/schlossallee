{
  "entities": {
    "GlobalSettings": {
      "title": "GlobalSettings",
      "description": "Global configuration and status for the Biergarten",
      "type": "object",
      "properties": {
        "isOpen": {
          "type": "boolean",
          "description": "Whether the Biergarten is currently open"
        }
      },
      "required": ["isOpen"]
    }
  },
  "firestore": {
    "/settings/{settingId}": {
      "schema": {
        "$ref": "#/entities/GlobalSettings"
      },
      "description": "Stores global settings like the current open/close status."
    }
  }
}
